{% extends 'base.html.twig' %}

{% block title %}Make Payment{% endblock %}

{% block body %}
    {{ parent() }}
    {% $order = new commande();  %}

    <form action="{{ path('checkout', {'id': commande.id}) }}" method="POST">
        <button type="submit" class="btn btn-primary btn-lg">Checkout</button>
    </form>
{% endblock %}