{% extends 'base.html.twig' %}

{% block title %}Confirmation de la commande{% endblock %}

{% block body %}
    {{ parent() }}

    <h1>Confirmation de la commande</h1>

    <p>Votre commande a été créée avec succès !</p>

    <h2>Détails de la commande</h2>
    <ul>
        {% for ligne in order.lignecommande %}
            <li>{{ ligne.livre.titre }} - Quantité: {{ ligne.quantite }}</li>
        {% endfor %}
    </ul>

    <h3>Montant total: {{ order.getTotal() }} TND</h3>

    <p>
        Si vous souhaitez payer en ligne, cliquez sur le bouton ci-dessous :
    </p>
    <form action="{{ path('checkout', {'id': order.id}) }}" method="POST">
        <button type="submit" class="btn btn-primary">Payer en ligne</button>
    </form>

    <p>
        Si vous préférez payer à la livraison, veuillez appeler le numéro suivant : 123456789
    </p>
{% endblock %}