{% extends 'base.html.twig' %}

{% block title %}Liste des utilisateurs{% endblock %}

{% block body %}
    <h1>Liste des utilisateurs</h1>
    <table class="table">
        <thead>
            <tr>
                
              
                <th>Nom</th>
                <th>Prénom</th>
                <th>Adresse</th>
                <th>Code Postal</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
                <tr>
                   
                   
                    <td>{{ user.nom }}</td>
                    <td>{{ user.prenom }}</td>
                    <td>{{ user.adresse }}</td>
                    <td>{{ user.codepostal }}</td>
                    <td><a href="{{ url('admin_user_show', {'id': user.id}) }}">
    <button type="button" class="btn btn-success">Voir Plus...</button>
</a></td>

                    <td><a href="{{ url('admin_user_delete', {'id': user.id}) }}">
                            <button type="button" class="btn btn-danger">Supprimer</button>
                        </a></td>
                    <td><a href="{{ url('admin_livres_update', {'id': user.id}) }}">
                            <button type="button" class="btn btn-info">Editer</button>
                        </a></td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
